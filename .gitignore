# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Packaged installers produced by `npm run build` / `npm run release`
# (the source of truth is src-tauri/target/release/bundle/, this is
# just the friendly-named copy — see build-scripts/build.js)
/release

# Local-only release secrets (signing key password, GitHub token) —
# see RELEASE.md. Never commit this file.
/.release-secrets.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
